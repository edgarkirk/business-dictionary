spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/businessdictionary}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
